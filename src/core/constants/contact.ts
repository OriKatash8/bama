/**
 * BAMA's contact address, shown at the end of a project's closing message.
 * MIRROR of BAMA_CONTACT_EMAIL in functions/src/lifecycle/closingNotice.ts —
 * the server writes it into the message; this is the fallback for a message
 * that carries none.
 */
export const BAMA_CONTACT_EMAIL = 'bama.app.hk@gmail.com';
